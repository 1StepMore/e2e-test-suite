name: fidelity

on:
  push:
    branches: [main, master]
    paths:
      - 'tests/fidelity/**'
      - 'test_corpus/real/**'
      - 'test_artifacts/ol/**'
      - 'Makefile'
      - 'Omni_Localizer/src/ol_mcp/**'
      - 'Omni_Localizer/src/ol_cli.py'
  pull_request:
    branches: [main, master]
    paths:
      - 'tests/fidelity/**'
      - 'Makefile'
      - 'Omni_Localizer/src/ol_mcp/**'
  schedule:
    - cron: '0 2 * * 1'
  workflow_dispatch:

jobs:
  fidelity-gate:
    name: Fidelity gate (char_cosine >= 0.9)
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Setup Python 3.13
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - name: Install sacrebleu
        run: pip install --upgrade sacrebleu pip pydantic-settings python-multipart cryptography starlette aiohttp
      - name: Verify candidate exists
        run: |
          if [ ! -f test_artifacts/ol/alice_ch1_zhipu_real.md ]; then
            echo "ERROR: candidate test_artifacts/ol/alice_ch1_zhipu_real.md not found."
            echo "Run 'make fidelity-nightly' locally (requires ZHIPU_API_KEY) and commit the result."
            exit 1
          fi
      - name: Run fidelity gate
        run: python tests/fidelity/run_fidelity.py

  fidelity-nightly:
    name: Fidelity nightly (regenerate via Zhipu)
    runs-on: ubuntu-latest
    timeout-minutes: 60
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Setup Python 3.13
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - name: Install sacrebleu
        run: pip install sacrebleu
      - name: Install OL for translate_md_text
        run: |
          git clone --depth 1 https://github.com/1StepMore/Omni_Localizer.git /tmp/ol
          pip install -e "/tmp/ol[mcp]"
      - name: Regenerate candidate + score
        env:
          ZHIPU_API_KEY: ${{ secrets.ZHIPU_API_KEY }}
          HF_ENDPOINT: 'https://hf-mirror.com'
          PYTHONPATH: /tmp/ol/src
        run: |
          python tests/fidelity/regenerate_candidate.py
          python tests/fidelity/run_fidelity.py
      - name: Upload result
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: fidelity-result
          path: tests/fidelity/results/alice_ch1_fidelity.json
