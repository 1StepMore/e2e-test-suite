# Omni Suite — Cross-Repo Release Notes

This file aggregates notable changes across all 4 repos (Suite + OPP + OL + ORF)
on a per-month basis. The `scripts/bumpversion.py --release-notes` command
generates draft content from the 4 CHANGELOG.md files.

## 2026-06

### Suite (`e2e-test-suite`, v0.2.0 → v0.4.0)

- (see `CHANGELOG.md` for full entries; this is an aggregate index)

### OPP (`Omni_Pre_Processor`, v0.6.0 → v0.9.1)

- (see `Omni_Pre_Processor/CHANGELOG.md`)

### OL (`Omni_Localizer`, v0.5.0 → v0.5.9)

- (see `Omni_Localizer/CHANGELOG.md`)

### ORF (`Omni_Re_Formatter`, v0.4.10 → v0.4.16)

- (see `Omni_Re_Formatter/CHANGELOG.md`)
