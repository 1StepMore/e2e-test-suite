# 交接文件 — 2026-09-18（Omni Suite 健康度报告结论落地）

> 面向接手的 coding agent。**唯一目标（用户 `/goal`，跨轮次持久）**：
> 根据 `docs/project-health-report-2026-09-17.md` 的结论优化项目；**复杂任务先 plan 后 implement**。
>
> 工作区根：`d:\贯维\Omni_Suite`（Windows / Git Bash 位于 `D:\Git\bin\bash.exe`）。

---

## 0. 一句话现状

- **suite 仓库**：工作树**干净**，最新 commit `60d4b43`。
- **三个子仓库**：**有本轮未提交的改动**（OPP 8 个源文件 + CI + pre-commit；OL CI + pre-commit；ORF pre-commit）。**接手第一件事就是验证并提交它们**（见 §3）。
- 报告项**已全部闭环**，仅剩 2 条外部阻塞（见 §4）。

---

## 1. 已完成（按时间倒序，含 commit 与证据）

### 本会话（2026-09-17 晚 ~ 09-18）

| commit | 仓库 | 内容 | 关键证据 |
|---|---|---|---|
| `60d4b43` | suite | `coverage_audit.py`：所有模块服务器都起不来时补一行 `ROOT CAUSE`（判定不动，仍 fail-closed exit 1） | `pytest tests/validation/test_coverage_audit.py -q` → 26 passed, 1 failed（该 1 个为环境类，stash A/B 证明 HEAD 同样失败） |
| `3dcd304` | suite | `CONTRACT.md` 新增 `Suite ↔ Module In-Process Import Surface` 节 + `tests/contract/test_contract_documentation.py` 两条绑定测试 | 22 passed；`doc_inventory --check` 先红（SKILL.md 行数声明 180→224）后绿 |
| `feaaa8c` | suite | 删除 3 条**永不生效**的 `extend-exclude` + 报告 §18 因果更正 | 见 §5「三处自我更正」第 2 条 |
| `f3c1739` | **OPP** | `src/opp/extractors/` 34 条潜伏 ruff 债清零（18 自动 + 16 手修） | `ruff check src/opp/extractors`（pin 0.15.11）→ 0；stash A/B 零回归 |
| `b84df45` | suite | 根目录样本守卫（`check_root_samples`）+ `Makefile` 三处裸 `python3` 清零 | 真实仓库注入样本 → rc 1 并指名文件；fixture 目录 → rc 0 |
| `9e9c11b` | suite | 两项既有红测：Windows 参数化 id（`WinError 123`）、OPP 平台反向 `xfail` 的过期断言 | `pytest` 三文件 → 38 passed |
| `fadd7cf` | suite | `make doctor` 静默 `exit 49` 修复 + `doctor.yml` 转阻塞 | HEAD 版脚本 1 行 + rc 49；修复后 7 项全跑到 + rc 0 |

更早轮次（`b99b5f7` §3.7 仓库卫生 / `242a4e2` / `893df81` 三条死正则 / `aa766da` ADR 0007 Phase 1 / R2 路径策略收敛等）见报告 §10–§14。

### 本轮（未提交，已改完并已验证）

| 文件 | 改动 | 验证 |
|---|---|---|
| `Omni_Pre_Processor/src/**`（8 文件） | F 类清零：11 条自动修（未使用导入）+ 2 条手修（`commands/batch.py` 的 `detected_format`、`logger.py` 的 `json_mode`；两处均已核实为纯死变量、无副作用、无外部引用） | `ruff check --isolated --select F src` → **All checks passed** |
| `Omni_Pre_Processor/.github/workflows/ci.yml` | **新增 lint 门禁**（此前该仓库无任何 lint 步骤）：`uv pip install "ruff==0.15.11"` + `.venv/bin/python -m ruff check --select F src/`，并写明范围理由 | YAML 解析通过；门禁**开箱即绿**（src F=0） |
| `Omni_Localizer/.github/workflows/test.yml` | lint 作业 `pip install ruff`（**未固定版本**）→ `"ruff==0.15.16"`（= 其 `uv.lock` 解析版本） | YAML 解析通过；lock 实测 `version = "0.15.16"` |
| `Omni_*/.pre-commit-config.yaml` ×3 | `check-secrets` 钩子 `python3 scripts/check-secrets.py` + `language: system` → `python scripts/check-secrets.py` + `language: python`（消掉跨平台裸 `python3`，与套件 §15 同类缺陷） | YAML 解析通过（曾一度改坏缩进，已修） |

**OPP 全量测试零回归**：改动前后均 `10 failed, 1035 passed, 37 skipped`（10 个失败是 `.venv_win` 缺 `nbformat` 等可选依赖的环境类，在 HEAD 上同样失败）。

---

## 2. 这个项目的「上下文陷阱」（务必先读，能省几小时）

1. **子仓库被 `.gitignore` 忽略**（`.gitignore:6-8`）→ `Grep`/`rg`/`ruff check .` **默认看不到** `Omni_*/` 里的文件。查子仓库必须用 `Select-String` / `Get-ChildItem` / 直接 `Read`。
2. **ruff 的 `extend-exclude` 不作用于显式传入的路径**（默认 `--no-force-exclude`）；而 `respect_gitignore = true` 会让全树运行跳过整个子仓库。**全树 lint 从来不覆盖子仓库源码**。
3. **pin 住的 ruff** 在 `C:\Users\RenAnZai\.cache\pre-commit\repo8fs3pr62\py_env-python3\Scripts\ruff.exe`（0.15.11，与 pre-commit/CI 同版本）。全局 `ruff` 是 0.16.8，**规则集不同**（会多报 `UP045`/`B023` 等 20 条）；判门禁一律用 pin 版本。
4. **原生 Windows 上 `.venv_ol` 不可执行**（Linux venv）→ 一切 spawn `.venv_ol` 的测试/脚本报 `WinError 1920`/`193`/`9009`。这是环境类失败，**不是回归**；`docs/dev/validation-loop-log.md:29` 有记录，CI（Linux）为准。
5. **本机 Python 解释器**：`.venv_win\Scripts\python.exe`（Windows 布局，可跑套件层测试）。裸 `python3` 在 Git Bash 下命中 Microsoft Store 占位程序 → **exit 49 且零输出**。
6. **`make` 未安装**；`gh` 未安装（无法查 CI run 历史）；`api.github.com` 匿名配额常耗尽。
7. **提交必须走真实钩子**：禁止 `--no-verify`；钩子失败要定位根因。套件 pre-commit 已安装，**三个子仓库未安装**（`.git/hooks/pre-commit` 是手写 secrets 回退脚本，非框架生成）。
8. 子仓库提交时**不要 `git add batch_test/`**：那里有历史遗留的 `normal_generated.md`（既有 dirty）与测试副产物 `normal.skeleton.zip`（未跟踪）。

---

## 3. 接手第一件事：提交并补完本轮

### 3.1 待提交（按仓库分组，用户明确要求「提交，按仓库分组」）

```bash
# OPP（注意排除 batch_test/）
git -C Omni_Pre_Processor add src .github/workflows/ci.yml .pre-commit-config.yaml
git -C Omni_Pre_Processor commit   # 用 -F <file> 传消息（PowerShell 不支持 heredoc）

# OL
git -C Omni_Localizer add .github/workflows/test.yml .pre-commit-config.yaml
git -C Omni_Localizer commit -F <msg>

# ORF
git -C Omni_Re_Formatter add .pre-commit-config.yaml
git -C Omni_Re_Formatter commit -F <msg>
```

提交消息要点：OPP = 「补 lint 门禁（此前完全没有）+ src F 类清零，含范围理由与 265/132 的对照数字」；OL = 「lint 依赖从 unpinned 固定到 uv.lock 的 0.15.16」；三仓库 = 「check-secrets 去掉裸 python3（language: python）」。

### 3.2 未完成的验证（**必做**）

我正要跑但被打断：验证三仓库的 `check-secrets` 钩子在 `language: python` 下**真能执行**（含旧写法对照）。

- 脚本已备好：`99-Tools/validation-scratch/omni-suite/probe_subrepo_hook.sh`（对三仓库逐个 `pre-commit run check-secrets --files scripts/check-secrets.py`，并打印 `python3 --version` 的 rc 作对照）。
- 跑法：`& 'D:\Git\bin\bash.exe' -lc "bash '/d/贯维/Omni_Suite/99-Tools/validation-scratch/omni-suite/probe_subrepo_hook.sh' > <out.txt> 2>&1"` 然后 `Get-Content <out.txt>`（**不要**把输出直接接在 `-lc` 后面：本机 Git Bash 的 stdout 重定向会报 `Bad file descriptor`）。
- 注意：`language: python` 需要 pre-commit 现场建 env（本地操作，无需联网）；若某仓库跑不起来，记录真实输出而不是断言它坏了。
- 若钩子验证通过 → 把结论并入报告 §21；若失败 → **不要提交该文件**，先定位（很可能是 pre-commit 需要 `language_version` 或本机 venv 创建受阻）。

### 3.3 报告 §21（未写）

在 `docs/project-health-report-2026-09-17.md` 末尾追加「第二十一节：子仓库门禁补齐」，必须包含：

- **第四处自我更正**：§18.4/§19.5 曾写「lint/test 门禁只存在于套件侧」——**错**。实测：OPP 无 lint（只有 pytest），OL 有 `ruff check src/ tests/`（但版本未固定），ORF 有 `ruff check src/orf/`（pin 0.15.14，**与其 uv.lock 一致**，故本轮不动它——改它要动 `uv.lock`，正好撞索引 403 的外部阻塞）。
- 本轮实测数字：OPP `src` 在 F 类下 13 条（已清零）；OPP `tests` 仍有 **132** 条 F（含 1 个 invalid-syntax）→ 门禁范围只取 `src/`；OPP `src` 在套件全规则集（E,F,I,B,UP,N）下仍有 **265** 条 → 全规则集门禁会先天红，故取 F 类。
- 同时更新各节的「遗留」表（§14.3/§15.5/§16.4/§17.4/§18.5/§19.5/§20.4）指向 §21，并在文末 footer 加一行。
- 提交前必跑 `bash scripts/pre_commit_python.sh scripts/doc_inventory.py --check`（改 docs 会触发 doc-truth 门禁；若报 SKILL.md 行数声明漂移，同步 `.opencode/skills/omni-docmap/SKILL.md` 后重跑）。

---

## 4. 目标状态：还剩什么

报告 §5 十条 + §3.x 全部闭环，**仅剩 2 条外部阻塞**（不得伪绿，见 `scenarios/STANDARDS.md#fallbacks-never-evidence`）：

| 项 | 阻塞原因 | 解锁条件 |
|---|---|---|
| §5 #2 PathValidator Phase 2（抽 `omni_security` 共享包） | 改依赖需重生成 `uv.lock`，索引返回 403 | 用户环境能访问 PyPI 索引后，按 `docs/adr/0007-path-security-convergence.md` 执行 |
| §5 #8 T13-01/T13-02 tier-2 真 LLM 复验 | 本机无 LLM key | 提供 `ZHIPU_API_KEY` 等后跑 tier-2 场景 |

报告之外、我自发现且**仍未做**的候选（做完上面再考虑，需先向用户确认口径）：

1. **三个子仓库未安装 pre-commit**（`.git/hooks` 是手写脚本）→ 可把 `pre-commit install` 接进 `scripts/setup_dev.sh` / `setup_dev.ps1`；注意会与现有手写钩子冲突（pre-commit 会把旧钩子挪到 `.legacy`）。
2. **OPP `tests/` 132 条 F 债**、OPP 无 `[tool.ruff]`（本机跑 `ruff check` 会落到套件根配置，导致「本地规则集 ≠ 子仓库独立克隆时的规则集」）。
3. **ruff pin 升级**：0.16.8 相对 pin 的 0.15.11 多报 20 条（含 `B023` 闭包捕获循环变量 ×4，属真实 bug 类）。

---

## 5. 三处必须知道的自我更正（报告内已留痕，不要重复踩）

1. **§19.1**：报告称 `scripts/mcp_bridge.py` 与各模块 MCP server 职责重叠 → 该文件 **2026-06-22 已删**（`5d163d4`），报告属转述旧印象。
2. **§18.1**：我一度判断 `extend-exclude` 让核心转换代码对门禁隐形 → **错**。真因是 `.gitignore` + `respect_gitignore`；那三条 exclude 是**永不生效的死配置**（已删）。
3. **§20.2**：我曾说 `coverage_audit.py` 把传输问题误诊为覆盖缺口 → **错**（只看了输出尾部）。逐模块 `SERVER ERROR` 行一直在，且 fail-closed 是刻意设计；本轮只补诊断行。

**教训（本项目的审计标准）**：结论一律以实测为准，**不得转述**；看到截断的输出不要下结论。

---

## 6. 常用复现命令

```bash
# 门禁（本机可用）
bash scripts/pre_commit_python.sh scripts/doc_inventory.py --check     # 文档真相门禁
bash scripts/check_deps.sh                                            # 7 项依赖体检（CI 同款 env 见 .github/workflows/doctor.yml）
.vnev_win/Scripts/python.exe -m pytest tests/ -m "not nightly" -q      # 套件层测试（.venv_win）

# 子仓库（Grep 看不到，用这些）
git -C Omni_Pre_Processor status --short
& "$env:USERPROFILE\.cache\pre-commit\repo8fs3pr62\py_env-python3\Scripts\ruff.exe" check --select F Omni_Pre_Processor\src

# 报告与 run 记录
cat validation-runs/latest.txt
python scripts/validation/validation_report.py validation-runs/<ts>/scenarios.json
```

一次性取证脚本都在 `99-Tools/validation-scratch/omni-suite/`（该目录被 gitignore，不进库）。
